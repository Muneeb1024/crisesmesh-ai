# Task 2.3 — Connect Mobile Report to Backend

**Prompt:**
Connect the `CitizenReportScreen` to the FastAPI backend. Use `httpx` or `fetch` to send the report data to `POST /api/v1/citizen/reports`. Update the UI to show a success message and the report ID once submitted.
