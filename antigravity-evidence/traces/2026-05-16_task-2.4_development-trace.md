# Task 2.4 — Show Citizen Report in Government Module — Trace

## Date: 2026-05-16

---

## Steps

### 1. Updated GovernmentHomeScreen.tsx
- Added `listIncidents` API import
- Added `incidents` state with `useEffect` fetch on mount
- Added `setInterval(fetchIncidents, 10000)` for 10s auto-poll
- Active Incidents panel: count = `activeIncidents.length` (live)
- Sub-text: `latestIncident.type — latestIncident.severity` (live)
- Alert Approval panel: count = `incidentCount` (live)
- Map status: `{incidentCount} active incidents • {totalReports} citizen reports`

### 2. Updated GovernmentIncidentScreen.tsx
- Added `useRoute` to read `incidentId` from navigation params
- Added `useEffect` to fetch incident by ID from backend
- Maps API response to local UI format, falls back to mock data

### 3. Tested E2E
- Citizen submits Water Logging report → report_005
- Government refreshes → Active Incidents: 4→5 ✅
- Backend API confirms 5 incidents total

## Key Decisions
1. **10s polling** — auto-refresh for live command center feel
2. **Fallback to mock** — incident detail shows hardcoded data if API fails
3. **Dynamic pluralization** — "1 incident" vs "5 incidents"
